# v2.3.0 全量升级发行准备

用户决定：版本 2.3.0，全部已完成升级随版发布；不自动推送/上传。

## 本地候选已生成 — 2026-09-19

- 产物：`dist/v2.3.0-release-20260919/editions/`，轻量、完整、附加 ZIP 及完整/附加分卷已生成。
  主 EXE 文件/产品版本均为 2.3.0；仅生成 v2.2.2 → v2.3.0 lite/full 两个增量包。
- 开发 `mods/enhancement` 仍为旧 worker，首次组装在复制前被哈希检查拦截；随后只读复用
  v2.2.2 正式 full 基线中的 `mods/enhancement`（SHA256 `17650318624cbf75604ee550bbd73203cb434f4dc59e82cacde1c84685d8aec0`）。
  未覆盖开发目录，未降低锁定校验，也未重新构建/下载推理环境。
- 三包 CRC、full 等于 lite+addon、分卷重组摘要、两附件 `--check-upload` 通过；源文件、editions、
  github-assets 三份 Release Notes 摘要一致。冻结基础 DLSS、轻量缺组件拒绝、full/叠加安装及 RAFT 测试通过。
- 新冻结更新助手的小型合成事务通过，设置保留及备份字节核验成功。两个真实增量包逐文件流式叠加还原目标也通过，
  未对旧版基线执行安装修改。本轮最终 70 项定向单测通过；不是全套 GPU 性能重测或干净机器认证。
  报告归档于 `output/package-v230-release-20260919-verification/`：`helper-report.json`、
  `update-lite-verification.json`、`update-full-verification.json`。
- 额外 1080p 四帧 HDR 冻结检查没有通过：首次日志出现显存余量不足，再次运行 240 秒超时，未生成最终结果。
  不以此断言 PyAV 损坏，也不声称 HDR 冻结验收通过；相关日志归档于上述目录的 `hdr-attempt-logs/`。
  候选包可以本地试用，但尚不作为全面验收通过的正式发行基线；未登记 v2.3.0 策略基线，未上传。
- 本地模式保留 `local_candidate=true`、`gpu_export_distribution_review_complete=false`，
  不是通过改审查标记冒充已批准发布。详细大小和摘要见 `editions/package-report.json`。

## 当前执行决定 — 2026-09-19

- 维护者明确只支持上一个正式版增量：本次为 v2.2.2 → v2.3.0 的 lite/full 两个包。
  已停用 v2.2.0/v2.2.1 条目，不恢复其已删除目录。`previous-release` 策略在下一版自动选择最新旧版。
- 新策略真实基线预检通过；策略、打包与本地材料检查相关 33 项测试通过。
- 本地打包与公开分发确认分离：`build_release.ps1 -LocalCandidate` 在核验材料和已安装媒体 DLL
  摘要后允许本地候选构建，普通发布构建仍保留原审查检查。不改 manifest 为已通过，
  不更换 PyAV/GPU 二进制，候选包及报告保留待确认标记；不自动上传。
- 构建输出 `dist/v2.3.0-release-20260919/`，预计新增峰值不超过 35 GiB，任务构建缓存不超过
  1 GiB；本轮开始 F 可用约 155.71 GiB、tmp 约 28.725 GiB。复用已有依赖，流式验证避免第二套解压环境。
- 以下多版本要求、下载失败和阻塞描述是过程记录，不再作为当前任务要求。

## 最新状态 — 2026-09-19

本节以下为前次记录；本次续跑结果优先见下方「再次打包预检」。

- 源码升级已本地提交为 `23d7372`，材料收集脚本包含在此提交中，并非漏交。
  后续下载处理、头文件许可证提取、zlib 来源固定分别提交为 `ab458e9`、`dc971c9`、`22c8032`。
- 最新材料收集测试 4 项通过；本次修复不修改 PyAV、GPU 运行库或已完成的功能验收结果。
- 18 个源码归档及已提取通知、构建输入保留在 `runtime/gpu-export/licenses/`，
  当前总计 187,306,272 B（约 178.63 MiB）。这是待审材料，不是已批准的发行清单。
- zlib 官网及官方 GitHub 下载均因 TLS 连接错误失败；唯一临时下载文件已由脚本清理。
  最终 `materials.json` 尚未生成，不能宣称材料收集完成。
- 两个本轮测试临时目录已清理；任务目录剩余约 30 KiB 的记录及小型材料，
  与上述持久材料一起保留供续跑，2026-09-26 复核。不重复下载已经验证的归档。
- 尚未构建 v2.3.0 整包或六个增量包，未完成冻结包和上传集合验收，未登记新版基线，未发布。

## 再次打包预检 — 2026-09-19

- zlib 官网连接恢复，下载 1,502,830 B，固定 SHA256 校验通过；材料收集器退出码 0。
  `runtime/gpu-export/licenses/materials.json` 已生成，记录 19 份源码、25 个媒体 DLL；
  材料目录共 188,864,047 B（约 180.11 MiB）。下载问题已经解决，不再列为当前阻塞。
- `load_components()` 校验成功，未改 PyAV/GPU 二进制。材料清单仍列出两个待审项：
  FFmpeg/x264/x265 链接分发条件、winpthreads 精确来源。收集完成不等于审查完成。
  `packaging/DLSS5Tool.spec` 第 39–40 行仍因 manifest 的审查标记为 false 主动终止构建；
  这是发行检查，不是已经发现 PyInstaller 编译失败或 GPU 功能回归。
- 新的 `release_updates.py --preflight-version v2.3.0` 退出码 1：
  `dist/v2.2.0-release-20260912/editions/package-report.json` 缺失。
  当前策略中的 v2.2.0 和 v2.2.1 目录均不存在；此前六套基线通过的记录不代表当前仍可用。
- 只读查询 GitHub：v2.2.0 正式轻量/完整/附加包附件仍可获取；v2.2.1 返回 `release not found`。
  未开始大文件恢复下载；未使用根目录历史同名 v2.2.0 实验 ZIP 代替正式基线。
  两个旧版基线需恢复或提供迁移路径；缩小支持范围必须由维护者明确决定。
- 当前 F 盘可用约 155.71 GiB，tmp 约 28.725 GiB；本轮仅增加少量材料和清单，
  未复制环境、生成发行包、改动更新策略或绕过任何检查。任务与材料保留至 2026-09-26 复核。

## 已处理

- `app_version.py`、Windows 文件/产品版本资源、中英文 README 源码版本统一为 2.3.0。
- 新增 `RELEASE_NOTES_v2.3.0.md`，相对上一个正式版 v2.2.2，包含序列输入、队列、预览、
  HDR 导出与诊断/原图对比改动；没有将既有插帧/RAFT 直连再次写成新功能。
- 初次 `release_updates.py --preflight-version v2.3.0` 失败，原因是正式 v2.2.2 full 解压目录
  多了 `dlss5_queue.json`、`dlss5_settings.json`。两者不在发行清单，未修改或扩展更新器白名单。
- 经授权将两个文件原样移到 `var/baseline-user-state-v230-20260919/`，搬移前后 SHA256 一致：
  queue `8a19922449f142fd8d4d9a88785e43b8299b53eae35361f9ff17471be8f4e951`；
  settings `7a0e511987544ea909ef2b19f073d9aba518d4635321a1692f8796295a43d2f5`。
  未删除用户数据，未更换正式发行文件。
- 再次严格预检通过：v2.2.0、v2.2.1、v2.2.2 的 lite/full 共六套正式基线全部匹配。
  支持策略未缩减，后续打包必须生成六个增量包。
- 打包/增量/更新器定向测试 38 项通过。旧增量测试夹具用 v2.2.0→v2.2.1 微型样本却默认读
  当前三版本生产策略，导致三项失败；现显式传测试自有策略，未修改生产支持列表或放宽校验。

## 新确认的实际发行材料问题

现有 PyAV 18.1.0 本身为 BSD-3-Clause，但 wheel 的 `av.libs` 实际包含 25 个 DLL，
其中包括 libx264/libx265。DLL 返回 `LGPL version 3 or later`，不能由此认定整个 wheel
都只有 LGPL 条款。

已核对固定上游：

- [PyAV v18.1.0 依赖锁定](https://github.com/PyAV-Org/PyAV/blob/v18.1.0/scripts/ffmpeg-8.1.json)
  指向 pyav-ffmpeg 8.1.2-1 Windows x86_64。
- [上游固定构建补丁](https://github.com/PyAV-Org/pyav-ffmpeg/blob/8.1.2-1/patches/ffmpeg.patch)
  将 libx264/libx265 从 EXTERNAL_LIBRARY_GPL_LIST 移到 EXTERNAL_LIBRARY_VERSION3_LIST。
  这解释了为什么加载这些组件时仍返回 LGPL 字样；补丁本身不能替代其他组件的许可。
- [固定依赖源码/哈希](https://github.com/PyAV-Org/pyav-ffmpeg/blob/8.1.2-1/scripts/pkg.py)。
- [FFmpeg 官方分发说明](https://ffmpeg.org/legal.html)要求区分实际启用组件，并提供匹配源码、
  修改和构建说明。这里记录事实，不是对项目整体许可兼容性的法律结论。

另：上游工作流从未固定 MSYS2 工具链包版本；本地 DLL 中 GCC 为 16.1.0，libiconv 1.19、
zlib 1.3.2 可读出。winpthreads 版本资源声明 MIT AND BSD-3-Clause，但不足以确定精确构建源码版本。

因此目前不能把 `packaging_license_review_complete` 改成 true。也不能只提供 PyAV 的 BSD 许可，
或只靠媒体 DLL 的 LGPL 返回字符串跳过检查。GPU 功能验收仍有效；问题是这套具体依赖的发行准备。

## 需要维护者决定的后续路径

1. 保持全部功能，改用来源/许可清晰的媒体封装依赖或独立封装进程，再做原有像素、帧序、
   时间戳、音轨、HDR 和取消对照验证。不能未经复测称为“相同已验收依赖”。
2. 保留当前二进制，由维护者完成 GPL/链接分发及现有专有组件兼容性判断，并配齐实际对应源码、
   构建材料和所有通知。不能由自动脚本替维护者作法律批准。

不采用删升级、降画质或单独翻转布尔标记方案。当前按用户要求保留现有功能及二进制，
继续补齐可核验的材料；上述替换方案未获授权，也未实施。材料收集与发行审查分开记录，
下载恢复不代表审查自动通过；未打包，不登记 v2.3.0 基线，不上传。

## 材料与复现

`scripts/prepare_gpu_export_materials.py` 是来源收集器：固定版本、解析 AST 而不执行上游 Python、
对上游提供的 SHA 进行校验，记录归档摘要，提取普通许可证文件及 SDK 头文件开头的许可注释；
下载有超时和重试，失败清理本次唯一临时文件，中断下载不作为成功材料。不会修改 GPU 运行库。
该收集器从不自动批准发行。正式材料尚未收集完整，不能宣称补齐。

续跑命令（复用已下载文件，有上游固定摘要的文件会重新核验）：

```powershell
.venv/Scripts/python.exe -B scripts/prepare_gpu_export_materials.py --work tmp/release-v230-20260919 --destination runtime/gpu-export/licenses
```

zlib 来源为官方 `madler/zlib` 的 v1.3.2 Release，源码包 SHA256 固定为
`bb329a0a2cd0274d05519d61c667c062e06990d72e125ee2dfa8de64f0119d16`。
下载完成后仍须核对最终清单与待审事项，再按 [固定发行流程](FILE_UPDATES.md) 完成构建、
三形态打包、六个增量包、冻结验收及 `--check-upload`；不得把本记录当作这些步骤的通过凭据。

任务：`tmp/release-v230-20260919/TASK.md`；无大包构建和模型/环境副本。
