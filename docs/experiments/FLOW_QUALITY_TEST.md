# 原片光流模型 A/B · 2026-09-09

## 结论

本片上 SEA-RAFT-M 有小幅代理指标优势，但没有证据支持“换模型后画质大幅提升”。当前 RAFT-Large 720/6 仍是合理的速度/质量基线。单纯把迭代增加到12不值得默认采用；1024分析尺寸的收益也明显小于耗时增长。

实际视频实验再次支持：给当前 Feature 18 提供光流可以改变输出，并降低本实验的增强时序残差。这个结论不是游戏引擎运动矢量等价性证明，也不是对所有视频的保证。

## 范围

- 原片：`C:\Users\PC\Desktop\视频输出\测试\9月1日.mp4`，1440×1440，165帧。
- 零基帧号窗口：0–15、64–79（包含截图附近帧74）、149–164；每窗独立reset。
- 每组48帧，每窗排除最初3帧，最终统计39对；7个不同条件＋基线复跑，共384帧DLSS输出。
- RTX 4070 SUPER；torch 2.8.0+cu128，torchvision 0.23.0+cu128；均FP32。
- 读取原视频进行本地测试，没有上传视频。官方代码、模型和额外依赖仅放入项目tmp测试目录，没有替换正式组件或修改保存设置。
- DLSS v2，RGBA8 / sRGB，固定外观参数（style=0、intensity=1、local_tone=1、local_struct=1、skin_struct=1、auto_mask=1）；无输出混合。不是GUI的全部颜色/缓存/显示路径复刻。
- 光流统一当前→上一帧，位移恢复到1440×1440像素单位，motion scale=(1,1)；额外计算前向场只用于诊断，不传入DLSS。
- RAFT使用项目原权重；SEA使用作者发布的Spring-M微调权重，720或1024为明确的分析尺寸，不额外套用官方benchmark的scale=-1（否则会再减半）。因此不是论文基准复现，也不是SEA所有权重的最优配置搜索。

## 实测结果

误差单位均为8位RGB灰度级，越低越好。不能直接称为光流EPE：真实视频没有真值光流。

| 模型 / 分析长边 / 更新次数 | 回投MAE：固定边界区域 | 回投MAE：共同可靠区域 | 时序残差：RAFT参考 | 时序残差：SEA参考 | 反向光流平均ms |
|---|---:|---:|---:|---:|---:|
| 无光流 | 6.223 | 6.655 | 1.7850 | 1.7705 | — |
| RAFT 720 / 6 | 2.907 | 2.642 | 1.4302 | 1.4212 | 61.5 |
| RAFT 720 / 12 | 2.925 | 2.651 | 1.4336 | 1.4245 | 86.8 |
| RAFT 1024 / 6 | 2.853 | 2.596 | 1.4202 | 1.4179 | 126.4 |
| RAFT 1024 / 12 | 2.884 | 2.602 | 1.4099 | 1.4116 | 168.8 |
| SEA-M 720 / 4 | 2.857 | 2.548 | 1.4189 | 1.3914 | 77.5 |
| SEA-M 1024 / 4 | 2.789 | 2.496 | 1.4130 | 1.3793 | 168.6 |
| RAFT 720 / 6复跑 | 2.907 | 2.642 | 1.4302 | 1.4212 | 63.8 |

- SEA720相对RAFT720：固定边界回投误差低约1.7%，共同可靠区域低约3.6%，增强时序残差低约0.8–2.1%；耗时高约26%（相对首次基线）。
- SEA1024相对RAFT720：固定边界回投误差低约4.0%，共同可靠区域低约5.5%，增强时序残差低约1.2–2.9%；耗时约2.74倍。
- RAFT720/6相对无光流：两套参考下增强时序残差低约20%。这里统计的是本片短窗口代理指标，不是“画质提升20%”。
- RAFT1024/12在RAFT参考下稍优于SEA1024，换SEA参考则顺序改变。这正是不能用某个模型自己的估计当唯一真值的原因。
- 基线复跑48帧的光流数组、DLSS输出全部逐像素一致。耗时仍有约4%的运行间差异。
- 1:1眼部/刘海截图没有显示明确的大幅画质跃升；未进行盲测或全片动态主观评分。并排视频只是辅助查看，不从有损视频反算指标。

## 指标边界

1. **回投MAE**：当前RGB与按反向场回投的上一帧RGB的绝对差。排除24像素边界及越界位置。光照、遮挡、运动模糊、压缩等仍会影响结果。
2. **前后向可靠区域**：`||b + warp(f,b)||² < 0.01*(||b||² + ||warp(f,b)||²) + 0.5`，且采样不越界。共同区域使用RAFT1024/12与SEA1024/4可靠区域交集，约占全图67.6%。各模型边界有效性会产生极小额外差异。
3. **增强时序残差**：`R=DLSS输出-输入`，比较`R_t`与回投后的`R_(t-1)`。分别固定RAFT1024/12和SEA1024/4为参考，对所有输出使用相同参考和交集mask。两个参考都不是真值，交集区域选择仍可能偏向参考模型。
4. 残差变小不自动等于画质提高；增强减弱也可能降低残差。本轮平均增强幅度为RAFT720 7.092、SEA720 6.955、SEA1024 6.970，没有退化为原图，但幅度变化仍是混杂因素。
5. 时间包含预处理、上传、推理、回传、位移resize，不含额外前向诊断、DLSS、编码、模型加载、IPC。这里采用独立脚本而非正式进程间管道，不能直接视为播放器FPS。测试前整卡约6.2GiB已占用；没有关闭用户应用。
6. 三个短窗口不覆盖长期漂移、多场景泛化或切镜。窗口内帧相关，不把39对当39个独立样本，不声称统计显著。

## 产物与复现

结果目录因F盘空间不足迁到本任务允许写入的C盘目录；原有两组结果保留，只删除过一个失败组的128字节数组头和空目录（可重新生成）。未清理用户历史文件。

输出目录：`C:\Users\PC\.codex\visualizations\2026\09\09\01a0856d-9185-7c40-b250-7ead570292f7\flow-quality-20260909`

- `input.json`：原片信息、帧号、输入SHA256、设置快照。
- `summary.json`：全部指标；`repeat.json`：精确重复验证。
- 每组`result.json`、`outputs.npy`、`flows.npy`、`masks.npy`、原尺寸抽帧PNG。
- `comparison-frame074.png`：固定量程光流与1:1裁剪。
- `comparison.mp4`：RAFT720/6、RAFT1024/6、SEA720/4并排；保留原窗口播放帧率，共48帧，窗口之间是人为跳转，无音频；已解码验证48帧。
- 脚本：`scripts/flow_quality_probe.py`、`scripts/flow_quality_boards.py`；2项CPU方向/缩放测试通过。

SEA代码commit：`9137517ba24e628442aec097d3afe71d03503b75`。
权重仓库revision：`eb97ef34ba5d856c3fa2cdcd073150c057ac8b69`。
权重SHA256：`cb8cfbf14c5e0f6734b64add383708b7ff68cc6089a0007c67165d4761346102`。

加载时跳过随即被checkpoint覆盖的ImageNet初始化下载；safetensors移除的共享BatchNorm别名，仅在验证`downsample[1] is bn3`后恢复，随后严格加载全部state。未忽略缺失学习参数，未更改官方forward。

本轮**没有测试WAFT**，也没有接入正式模型下拉框。SEA的微小优势不足以直接默认替换RAFT；若继续扩展，应增加其他实际视频、全片时间一致性和WAFT对照。

官方来源：[SEA-RAFT代码](https://github.com/princeton-vl/SEA-RAFT)、[作者权重](https://huggingface.co/MemorySlices/Tartan-C-T-TSKH-spring540x960-M)。
