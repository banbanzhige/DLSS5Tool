# 临时目录审计 · 2026-09-11

状态：**第一批已获用户批准并清理完成**（2026-09-11 15:59，UTC+8）。实际目录为 `F:/project/DLSS5Tool/tmp/`，不存在 `.tmp/`。
这里的问题是磁盘空间，不是 RAM；本次未诊断内存使用。

## 清理执行结果

- 仅删除下方第一批明确列出的 **41 个目录、32,645 个文件**，逻辑体积 38.089 GiB。
- F 盘可用空间从 **5.324 GiB** 增至 **43.461 GiB**，本次前后实测增加 **38.138 GiB**。
  这是整盘可用空间差值，受文件系统分配及其他进程活动影响，不等同于文件逻辑长度。
  随后最终复查可用空间为 **42.11 GiB**；整盘余量在清理完成后仍有变化，未扩大范围调查其他写入。
- `tmp/` 剩余 **28.688 GiB**。第二批、环境、模型、素材、所有明确备份及当天新组件未删除。
- 删除前检查绝对路径边界、逐层拒绝重解析点、Git 跟踪、代码引用、近期修改及逐文件独占打开；
  未发现相关运行进程、媒体/模型文件或占用冲突。每个目录删除前再次核对文件清单。
- 删除后核对 `tmp/` 内所有非目标条目和文件的路径、大小、修改时间快照，全部保持一致。
  当前 `mods/enhancement/guidance_worker.exe` SHA-256 前后相同：
  `2b309510ef6f73ae73dc98d11842a8ef3ba2e012c19a7e1cfe38def9a3b9f450`。
- 另存 **52 份**组件说明、构建警告等小文件，逐份校验 SHA-256；连同完整文件清单、
  可执行文件哈希、删除记录和执行脚本共约 **5.593 MiB**，位于本地
  `output/tmp-cleanup-20260911/`（不提交 Git）。机器可读结果为该目录的 `report.json`。
- 使用永久删除，**不经过回收站，未保留旧二进制完整副本**；小文件可从上述记录目录恢复，
  旧程序/依赖副本需重新构建或从已有发行归档取得，不能保证重现字节完全一致的历史包。
- 剩余空间仍超过守则 20 GiB 预警线；在下次大型构建前复核，最迟 2026-09-18 人工复核。
  未安排自动删除任务，也未扩展清理到第二批或仓库其他目录。

以下保留清理前的盘点依据。

- `tmp/` 文件逻辑长度合计 **66.778 GiB**，94,395 个文件；盘点时 F 盘可用约 **5.32 GiB**。
- 以下统计含隐藏文件；未发现目录联接/符号链接，`git ls-files tmp .tmp` 无跟踪文件。
- 只读进程检查未发现可执行路径或命令行包含本仓库绝对路径的进程。
  此检查不覆盖相对路径启动、其他任务工作目录或打开的句柄；实际删除前仍须复核。
- GiB = 2^30 字节。逻辑体积不是实际磁盘分配量；未做硬链接去重，不能保证释放值完全相同。

## 第一批候选：合计 38.089 GiB

以下是旧生成包和中间文件，不是字节级相同的“重复文件”。源码/构建流程可重建相关功能，
但不保证重建历史二进制完全相同。确认不再需要这些版本做 A/B 对比、保留必要报告与哈希，
且没有任务正在使用后，可按清单删除。**此清单已执行，结果见上方。**

### 旧增强组件候选：31.235 GiB

当前安装组件独立位于 `mods/enhancement/`；打包记录已记载更新的 v2.1.3 组件与验证。
今天的 `unlocked-component-20260911/` 和所有明确命名的备份均不在第一批中。

| `tmp/` 下的目录 | GiB |
| --- | ---: |
| `guidance-cache-20260908` | 4.433 |
| `guidance-sdpa-20260908` | 4.433 |
| `guidance-shm-final-20260908` | 4.433 |
| `guidance-shm-20260908` | 4.433 |
| `first-pass-input-20260909` | 4.433 |
| `nvofa-component-20260910-r1` | 4.536 |
| `nvofa-grid-component-20260910` | 4.536 |

### 旧基础包解压/验证目录：5.642 GiB

下列 13 个目录各约 0.434 GiB，内含旧 `DLSS5Tool-v2.0.1` 运行目录：

```text
raft-streams-base-20260908
sdpa-base-final-20260908
sdpa-base-20260908
shm-base-20260908
mods-compact-20260908
gpu-base-20260908
addons-default-layout-20260908
mods-autodetect-20260908
portable-base-test
mods-layout-20260908
mods-package-verified
mods-package-final
mods-package-check
```

删除前确认其中没有手动添加的设置、媒体或尚未归档的验证结果；历史文档中引用的旧命令将不再可直接运行。

### 旧构建中间目录：1.212 GiB

```text
addons-default-layout-build
first-pass-input-build-20260909
gpu-base-build
guidance-cache-build
guidance-cuda-build
guidance-original-raft-streams-build
guidance-parameters-build
guidance-raft-streams-build
guidance-sdpa-build
guidance-shm-build
mods-autodetect-build
mods-build-check
mods-build-final
mods-build-verified
mods-compact-build
mods-layout-build
portable-base-build
portable-enhancement-build
raft-streams-base-build
sdpa-base-build
shm-base-build
```

这是本次枚举后的明确清单，不应转成 `*build*` 通配符删除命令。
初步估计第一批清除后约 28.689 GiB；执行后实测为 **28.688 GiB**。

## 第二批：单独确认，不默认清理

| 对象 | GiB | 保留/复核理由 |
| --- | ---: | --- |
| `guidance-pre-sdpa-20260908`、`guidance-pipe-backup-20260908` | 8.865 合计 | 文档明确标注为完整回滚备份，需确认保留哪一个 |
| `guidance-probe-env` | 3.199 | 独立 CPU RAFT 环境；历史探针复现需要，不等于无用缓存 |
| `unlocked-component-20260911`、`unlocked-build-20260911` | 4.536 合计 | 当日验证产物；确认新包验收与回滚来源后再处理 |
| `guidance-cpu-backup-20260908`、`portable-enhancement-test` | 1.174 合计 | CPU 备份/测试组件；相同大小不等于内容完全相同 |
| `xformers-probe-pypi` | 0.307 | 可选注意力对比依赖；确认不再复现实验后可重装 |

## 当前保留

- `guidance-cuda-env` **7.163 GiB**：打包脚本直接依赖，不能当作过期环境删除。
- `dlss5standaloneV2` **1.424 GiB**：模型源码、权重参考；多个脚本直接引用。
- `flow-quality-deps`、`flow-quality-sea-raft`、`img`、`hdr-e2e`、`realtime-routes-20260908`：现有脚本输入。
- SDK/研究资料、`pdfs`、单独脚本、日志/JSON、小型原始验证材料及其他备份：未核实可恢复来源，不纳入批量删除。
- 仓库外发行物，以及 `mods/`、`runtime/`、`.venv/`、`var/`、`output/`、`dist/` 均不在本次清理范围。

依据包括 `scripts/package_editions.py`、`scripts/guidance_probe.py`、
`scripts/flow_quality_probe.py`、[NVOFA 集成](../experiments/NVOFA_INTEGRATION.md)、
[构建发行记录](../release/PACKAGING_INDEX.md)、[当日限制解锁记录](PROCESSING_LIMITS_20260911.md)。

本次仅核对本仓库记录，未重新校验外部发行包。第一批执行遵循
[卫生守则](REPOSITORY_HYGIENE.md)；第二批仍需另行确认，不延用本次删除授权。
