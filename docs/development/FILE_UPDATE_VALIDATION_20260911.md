# 文件级更新实现验证 — 2026-09-11

## 范围与状态

完成版本对变化文件包生成器、客户端安全校验/下载/暂存、双重确认界面、独立 Windows 助手、逐文件备份与回滚、恢复入口及发布文档。未升级 APP_VERSION（仍 v2.1.3），未重新打包正式主程序/完整推理环境，未上传附件或创建 tag。发行步骤见 [FILE_UPDATES.md](../release/FILE_UPDATES.md)。

首版选择版本对差异 ZIP 容器（`.dlssupdate`），不构建零散文件 CDN；缺少匹配包或可信 API 摘要时回退整包。官方修改过的受管文件会阻断更新，不绕过兼容性检查。异常退出依靠状态和刷盘日志恢复，不承诺损坏磁盘或文件系统下的绝对原子性。

## 验证

- 环境：Windows 11 10.0.26200、Python 3.13.3、PyInstaller 6.22.2；复用仓库 `.venv`。
- 最终全量回归 `python -B -m unittest discover -q`：459 项，451 通过，8 跳过，无失败；耗时 123.028 秒。
- 专项 `test_delta_update/test_update_helper/test_gui_updates/test_updater/test_release_packaging/test_i18n`：66 项全通过。
- 收尾调整更新检查只读取 `_collect_host_settings()`，避免完整设置采集创建预览共享缓存；调整后 GUI 更新专项 8 项重跑全通过。全量回归与此调整重叠运行，故上述 8 项是该行调整的确定性补测。
- `git diff --check` 无空白错误，只有仓库现有 CRLF/LF 归一化提示。
- 真实 Windows `CreateFileW` 共享模式禁止删除/替换的 DLL 锁场景：安装失败后其他已变更文件恢复，原被锁 DLL 不强行覆盖，状态 `rolled_back`。
- 测试覆盖本地修改、缺文件、新路径冲突、错误 SHA、额外/重复归档条目、Windows 路径/ADS/设备名/大小写冲突、硬链接、取消、空间不足、替换异常、恢复重试与中断日志。GUI 测试为无窗口契约测试，不冒充屏幕实测。
- 独立助手冻结构建 9,356,689 B（约 8.92 MiB），SHA-256 `6b3d9a9a6eefddfe1d07dab6678f0adbbe6d54f846051862faa8280183f36978`。
- `scripts/probe_file_update.py` 使用该冻结助手，等待真实短生命周期父进程退出后，将合成 v9.0.0 文件替换为 v9.0.1；退出码 0，状态 `complete`，设置保留且旧 EXE 备份字节验证通过。探针不运行合成 EXE，不调用 GPU，也不访问 GitHub。
- 初轮测试捕获 Windows 只读文件句柄 `fsync` 失败，已改为可写句柄，并通过源码和冻结助手重验。

## 发行前仍需验证

- 分配新版本号并正式构建含助手的主程序；保留第一份支持文件更新的完整基线。
- 用真实冻结发行目录生成 lite/full 更新包，验证实际变化、体积、组件与主程序兼容性。
- 干净 Windows 环境、普通用户权限/杀软、GUI 实际下载与退出安装流程；发布后检查 GitHub API 提供的附件摘要与本地报告一致。
- 保留初次手动升级与完整包下载路径。当前变更不会让 2.1.1/2.1.2 等已经分发的旧程序自动获得此能力。

## 仓库卫生

所有构建/探针暂存在 `tmp/file-update-20260911`，无 Torch/CUDA 环境复制，无大型发行包生成。初始 F 盘约 107.65 GiB 可用；既有 tmp 约 29 GiB，本次预算 <128 MiB。脚本已保留至 `scripts/`，结论与助手哈希保留于本文件。交付清理本次明确创建的 `helper-build`、`helper-dist`、`frozen-probe`、`final-probe`，不动其他任务、推理环境、用户模型或旧备份；这些仅为可重建测试产物。

清理已完成：删除逻辑体积 97,012,507 B，F 盘清理前后可用空间增加 97,079,296 B（约 92.6 MiB）；清理后可用 115,595,587,584 B。本任务仅余 `TASK.md` 843 B，作为用途与重建登记保留至 2026-09-18 复核。删除的测试 EXE/合成备份不可从此目录恢复，但可用保留脚本重建；原始用户数据未删除。
