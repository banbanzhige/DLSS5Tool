# GPU export evidence

Main report: [GPU_EXPORT_INTEGRATION_20260919.md](../GPU_EXPORT_INTEGRATION_20260919.md).

`MANIFEST.json` enumerates report/media/log files, final source snapshots and SHA256.
Negative mux reports are retained. Historical reports identify source hashes at run
time; snapshots are final source versions, not a claim that every intermediate run
used the final script. Original 20260917 archives remain unchanged.

Primary positive reports:

- `container-timing.json`, `short-audio.json`, `pcm-audio.json`, `atomic-long-audio.json`.
- `fg2-mp4.json`, `fg3-mkv.json`, `fg4-mov.json`, `fg4-cut.json`.
- `cancel-reopen.json`.

No runtime replacement, default backend change, HDR GPU conversion acceptance,
long-duration performance claim, complete upgrade signoff or release packaging.
