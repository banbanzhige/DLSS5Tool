# 可选深度 SDPA＋FP16 集成 · 2026-09-08

> 2026-09-09：产品默认参数已按用户实测配置调整为深度 SDPA＋FP16／混合双 Stream，但 GUI 启动时推理关闭，启用前检查环境。已有显式配置保留。下文 FP32 默认值是本轮集成时的历史状态；当前行为见 [默认参数与启用检查](GUIDANCE_ACTIVATION.md)。

## 使用

重启开发版 `run.bat`，进入：

**引导 → 高级性能设置 → 深度加速 → SDPA + FP16（实验）**。

适用于 CUDA 下「仅深度」或「深度＋光流」；RAFT 光流仍为 FP32。
「仅光流」不受此选项影响，实际握手仍报告 FP32。
默认保持「原始 FP32」，本轮没有更改用户保存参数；选择后按既有规则自动保存。
如遇画面异常或显存不足，选回「原始 FP32」重试；不会自动降精度、换模型或切 CPU。
切换会停止旧预览任务、清理缓存并重建引导会话，队列／导出期间禁用修改。
等待当前帧结束和关闭旧引擎在后台执行，界面显示切换状态；完成后恢复预览。
切换期间禁用相关设置、导入和导出入口，避免旧会话尚未退出时并发重建。

### 切换卡死修复

旧实现的界面线程会 `join(5)` 等待预览线程，超时后继续等待 `_live_lock`；
预览线程持有这把锁通知模型就绪时，`root.after()` 又可能等待界面事件循环，造成互等。
现改为线程安全通知队列，由界面定时读取，并丢弃旧会话迟到的精度通知。
后台等待旧预览线程真正退出后再关闭旧引擎，界面不等待锁或子进程；
通用等待函数超时后保留旧线程引用，防止误启动第二个预览线程。
切换中请求退出会在旧引擎释放后关闭窗口。

回归测试：`tests/test_gui_module_reload.py`（模拟慢推理、慢关闭、迟到通知、
重复切换、失败恢复、退出与快捷键保护，以及真实隐藏 Tk 窗口的事件循环和双向控件恢复）。
这些 GUI 回归测试不加载真实 GPU 模型，不代表新的推理速度基准。

另外使用 `scripts/module_reload_probe.py --source 原片路径` 做真实隐藏窗口实测：
RTX 4070 SUPER、原片1440×1440、ViT-L、深度＋光流、长边720，
在首帧加载中切到 SDPA＋FP16，收到实际加速精度通知后切回 FP32，两个新会话均成功处理首帧。
一次实测切换回调约48ms／27ms，界面事件最大间隔294ms，无回调异常。
此数字仅为本机单次交互响应结果，不是模型加载耗时或吞吐保证。
脚本使用临时设置与队列文件，不更改用户保存配置，不导出或覆盖原视频。

按 UI/UX 技能的表单标签、持久提示和渐进展开规则，仅在现有进阶设置内新增一行，
持续显示实验性、数值变化、显存风险和回退方式；中英文与现有浅／深色主题一致。

## 实现

- `guidance_depth_profile`: `fp32` / `sdpa_fp16`，保存及缓存／进程合同包含该字段。
- `depth_acceleration.py` 将已加载 Depth Anything V2 的注意力方法适配为 PyTorch SDPA；
  校验 ViT-S/B 为12层、ViT-L为24层，只支持普通 eval 张量，拒绝训练或 nested bias。
- 不修改模型权重、引导尺寸或第三方源码，不引入 xFormers；仅深度 forward 使用 CUDA autocast FP16。
  输出先转回 FP32，再使用既有 CPU 深度归一化与共享内存输出。
- `GuidanceSession` 校验组件握手中的配置、实际注意力和精度，旧组件无法静默忽略加速请求。
- CPU 深度加速、旧组件、不兼容架构及深度 NaN/Inf 提供中英文恢复提示；不默默回退。
- 原始 FP32 路径继续兼容旧组件；新组件也保留共享内存和旧管道协商。

## 实际链路验证

原片 `9月1日.mp4`，1440×1440，165帧；RAFT-Large 6次更新、Depth Anything V2 Large，
引导长边720、共享内存、冻结 CUDA 组件＋隔离 ProcessLive＋NVENC H.264 p5。
两种配置各先预热，然后从原片首帧重新开始，逐帧解码、处理并编码。

| 项目 | 原始 FP32 | SDPA＋深度FP16 |
| --- | ---: | ---: |
| 准备／预热（单列） | 10.02秒 | 9.37秒 |
| 165帧解码＋处理＋哈希＋编码 | 71.59秒 | 40.85秒 |
| 对应吞吐 | 2.30fps | 4.04fps |
| 平均 process 调用 | 423.46ms | 237.15ms |
| 平均深度步骤 | 298.23ms | 96.37ms |
| 平均光流步骤 | 85.88ms | 100.81ms |
| Torch模型峰值分配 | 2569MiB | 3067MiB |

本轮整体吞吐约提升75%，计时阶段缩短43%。
这是单次顺序 A/B，后台GPU活动可能影响速度，不能承诺固定倍数。
包括源视频解码和测试哈希开销，不包括 GUI 或音频复用；不是点击 GUI 导出按钮的端到端自动化测速。
同步 process、host_in_flight=1；三槽异步正确性另测。

- 两个测试视频均完整解码165帧。
- 编码前逐帧 DLSS SHA-256 全部一致；不是比较有损视频文件哈希。
- 同一 ProcessLive 对象从加速切回 FP32，确认旧会话关闭、新会话实际 FP32 推理成功。
- 三种模式分别通过7帧同步／三槽异步逐像素一致性及 reset／非活动引导清零测试。
- 全量测试227项：224通过、3个Torch专用测试在基础环境中按设计跳过。
  另用CUDA构建环境运行这3项CPU数学／状态字典测试，全部通过。
- 中英文各10张浅／深色测试窗口截图，无回调错误；检查新增选项及提示可读。
- 新基础冻结 EXE＋已部署CUDA组件的加速模式诊断通过：`output/sdpa-frozen-result.json`。

速度、误差的先前多轮对照见 `ATTENTION_BENCHMARK.md`。
此片最终输出一致不代表深度图数值无损或所有素材无损；仍需按真实素材验证运动、遮挡和闪烁。
AMP在本配置的Torch峰值分配增加约498MiB，未承诺节省显存。

## 产物

- 新组件已部署：`mods/enhancement`。
- 部署前共享内存／FP32组件完整备份：`tmp/guidance-pre-sdpa-20260908`。
- 之前的旧管道组件备份仍在：`tmp/guidance-pipe-backup-20260908`。
- 新基础冻结测试包：`tmp/sdpa-base-final-20260908/DLSS5Tool-v2.0.1`；不内置模型／组件。
- 仍为本地验证组件，保留 `LOCAL-TEST-ONLY.md`，不是已完成许可审查的公开发行包。
- 原片报告／静音测试视频：`output/sdpa-integrated-video/`。
- 异步模式报告：`output/sdpa-integrated-smoke/report.json`。
- UI截图：`output/sdpa-ui/`。

复测脚本：

```powershell
.venv\Scripts\python.exe scripts\depth_acceleration_probe.py --component mods\enhancement --source "原片路径.mp4" --output output\sdpa-video-new
.venv\Scripts\python.exe scripts\guidance_transport_smoke.py --component mods\enhancement --output output\sdpa-smoke-new --depth-profile sdpa_fp16
```

必须使用新输出目录。模型权重、DLSS DLL、用户原片和保存参数均未更改。

## RAFT 精简撤回

后续实现的“仅生成最后一次上采样输出”虽然在先前有限抽帧和数组比较中数值一致，
但用户实拍素材连续播放出现严重时序抖动，因此已从生产代码与组件撤回。
`raft_final` 旧设置自动迁回原始串行；双 Stream 保留为独立实验选项，但其光流模型也恢复为
torchvision 原始 RAFT 的6次完整输出。有限抽帧不能替代连续播放的时序验收。

## 下一轮优化顺序

1. **RAFT输入／特征复用。** 保留上一帧GPU输入或特征，减少重复上传、预处理和特征提取；
   特征缓存需要在 seek、切镜、尺寸／精度／模型改变时失效，并验证单帧编码与双帧批处理的数值差异。
2. **光流混合精度。** 在原权重和迭代数下独立测试，重点检查细小运动、遮挡、拖影及NaN；不默认整网half。
3. 之后再考虑编译／推理引擎或跨API GPU共享。它们改动、打包和维护成本更高，需额外基准决定。

深度与光流现已接近同量级瓶颈，单靠某一个小优化不能承诺从4fps到30fps。
